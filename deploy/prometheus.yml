global:
  scrape_interval: 15s
  evaluation_interval: 15s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: energy-agent
    static_configs:
      - targets: ["energy-agent:8080"]
